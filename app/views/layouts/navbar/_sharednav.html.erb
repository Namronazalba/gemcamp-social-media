<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link link-info text-decoration-none" if user_signed_in? %>
        </li>
        <li class="nav-item">
          <%= link_to "Posts", posts_path, class: "nav-link link-info text-decoration-none" if user_signed_in? %>
        </li>
        <li class="nav-item">
          <%= link_to "Logout", destroy_user_session_path, class: "nav-link link-info text-decoration-none", :method => :delete if user_signed_in? %>
        </li>
        <li class="nav-item">
          <p class="nav-link link-info text-decoration-none"><%= current_user.email %></p>
        </li>
      </ul>
    </div>
  </div>
</nav>