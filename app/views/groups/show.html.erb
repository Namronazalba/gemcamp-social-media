<%= render "layouts/navbar/sharednav" %>
<div class="text-end">
  <%= link_to " edit ", edit_group_path(@group) if policy(@group).update? %>
  <%= link_to "delete", group_path(@group), method: :delete, data: { confirm: 'Are you sure do you want to delete?' } if policy(@group).update? %>
</div>
<div class="w-100 m-auto text-center p-2"><b><%= @group.name %></b></div>
<%= image_tag @group.banner&.url,style:"object-fit: none;object-position: center; width: 100%; max-height: 300px; margin-bottom: 1rem;" if @group.banner? %>
<div class="w-75 m-auto text-start p-2"> &nbsp;&nbsp;<%= @group.description %></div>
<hr>
<%= link_to "invite", new_member_path%>